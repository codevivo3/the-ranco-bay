# Canonical public URL, for example https://www.example.com
NEXT_PUBLIC_SITE_URL=

# Contact enquiry email delivery (server-side only).
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=

# Private Booking.com iCalendar export URL (server-side only; never NEXT_PUBLIC_).
BOOKING_ICAL_URL=

# Future Sanity connection. Leave empty until the CMS phase.
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=
SANITY_API_READ_TOKEN=
